"use client";
import Link from "next/link";import {useState} from "react";import "../portal.css";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const r=await fetch("/api/portal/password/reset-request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json().catch(()=>({}));setBusy(false);setMsg(d.message||d.error||"Check your email for the reset link.")}return <main className="portal-login"><section className="portal-login-card"><div className="portal-brand"><span>DP</span><div><strong>DAVIDPILOT</strong><small>Client Portal</small></div></div><span className="portal-kicker">ACCOUNT RECOVERY</span><h1>Reset password.</h1><p>Enter your client email and we will send a secure recovery link.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label>{msg&&<div className="portal-notice">{msg}</div>}<button disabled={busy}>{busy?"Sending…":"Send reset link"}</button></form><small className="portal-help"><Link href="/portal/login">← Back to sign in</Link></small></section></main>}
